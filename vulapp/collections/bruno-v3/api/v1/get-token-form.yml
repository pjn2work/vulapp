info:
  name: POST /api/v1/get-token-form
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseUrl}}/api/v1/get-token-form"
  body:
    type: form-urlencoded
    data:
      - name: username
        value: admin
      - name: password
        value: easypassword

runtime:
  scripts:
    - type: after-response
      code: |-
        bru.setVar("bearerToken", res.body.reply.token);

settings:
  encodeUrl: true
