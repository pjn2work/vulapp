info:
  name: GET /web/welcome-cookie
  type: http
  seq: 9

http:
  method: GET
  url: "{{baseUrl}}/web/welcome-cookie"
  headers:
    - name: Cookie
      value: "secret-cookie={{secretCookieValue}}"

settings:
  encodeUrl: true
