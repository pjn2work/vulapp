info:
  name: GET /web/oauth2/callback
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/web/oauth2/callback"
  params:
    - name: code
      value: "{{oauthAuthCode}}"
      type: query
    - name: state
      value: randomstate123
      type: query

settings:
  encodeUrl: true
