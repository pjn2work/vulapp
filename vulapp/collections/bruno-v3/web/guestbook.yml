info:
  name: GET /web/guestbook
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/web/guestbook"
  params:
    - name: name
      value: guest
      type: query

settings:
  encodeUrl: true
