info:
  name: GET /web/oauth2/profile
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/web/oauth2/profile"
  auth:
    type: bearer
    token: "{{oauthToken}}"

settings:
  encodeUrl: true
