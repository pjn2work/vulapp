info:
  name: GET /api/tools/logs
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/api/tools/logs"
  params:
    - name: lines
      value: "1000"
      enabled: true

settings:
  encodeUrl: true
