info:
  name: GET /api/tools/otp
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/tools/otp"
  params:
    - name: seed_b32
      value: "{{totpSeed}}"
      type: query

settings:
  encodeUrl: true

runtime:
  scripts:
    - type: after-response
      code: |-
        bru.setVar("otpCode", res.body.otp_code);
