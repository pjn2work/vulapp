info:
  name: GET /api/v1/header-cookie-auth
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/api/v1/header-cookie-auth"
  auth:
    type: basic
    username: admin
    password: easypassword
  headers:
    - name: secret-header
      value: "{{secretHeaderValue}}"
    - name: Cookie
      value: "secret-cookie={{secretCookieValue}}"

settings:
  encodeUrl: true
