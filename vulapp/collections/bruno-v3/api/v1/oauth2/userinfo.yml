info:
  name: GET /api/v1/oauth2/userinfo
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/v1/oauth2/userinfo"
  auth:
    type: bearer
    token: "{{oauthToken}}"

settings:
  encodeUrl: true
