info:
  name: GET /api/v1/is-valid-token
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/v1/is-valid-token"
  auth:
    type: bearer
    token: "{{bearerToken}}"

settings:
  encodeUrl: true
